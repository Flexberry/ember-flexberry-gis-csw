import { flexberrySearchPropertiesEsri } from 'dummy/helpers/flexberry-search-properties-esri';
import { module, test } from 'qunit';

module('Unit | Helper | flexberry search properties esri');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = flexberrySearchPropertiesEsri([42]);
  assert.ok(result);
});
